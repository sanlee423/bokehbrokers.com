import React from 'react';
import Popover from '@/components/popover';

const Main: React.FC = () => {
  return (
    <div className="text-center bg-black">
      <Popover />
    </div>
  );
};

export default Main;
